{
  "name": "argus-cli",
  "version": "0.1.0",
  "description": "DevSecOps security scanner — 100% local code analysis with TypeScript AST-based auto-fix",
  "type": "module",
  "bin": {
    "argus": "./bin/argus.ts"
  },
  "files": [
    "bin/",
    "src/",
    "tsconfig.json"
  ],
  "scripts": {
    "dev": "bun run bin/argus.ts",
    "build": "bun build bin/argus.ts --outdir=dist --target=node",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
