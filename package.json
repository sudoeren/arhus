{
  "name": "argus-cli",
  "version": "0.1.0",
  "description": "scan. fix. repeat. — local-first security analysis with TypeScript AST-based auto-fix",
  "type": "module",
  "bin": {
    "argus-cli": "./bin/argus.ts"
  },
  "files": [
    "bin/",
    "src/",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "bun build bin/argus.ts --outdir=dist --target=node",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.2",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.2",
    "ora": "^9.4.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
